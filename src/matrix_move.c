/* TODO: reminder: finish all the assignments on WeBeep
	remaining ones: 4 - 5 - 6 (this one) - 7
*/
#include <stdio.h>

#define N 4
#define K 7

enum movement
{
	BEHIND = -1,
	NONE,
	FRONT
};
struct move
{
	enum movement row;
	enum movement col;
};

int main()
{
	// struct move a[K];
	// TODO: finish this
	return 0;
}