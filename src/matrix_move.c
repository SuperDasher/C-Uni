/* TODO: reminder: finish all the assignments on WeBeep
	remaining ones: 4 - 5 - 6 (this one) - 7
*/
#include <stdio.h>

#define N 4
#define K 7

enum move_flag
{
	BEHIND = -1,
	NONE,
	FRONT
};
struct move
{
	enum move_flag row;
	enum move_flag col;
};

int main()
{
	struct move a[K];
	// TODO: finish this
	return 0;
}